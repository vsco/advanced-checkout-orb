version: 2.1

description: >
  Advanced Checkout Orb for CircleCI. By default attempts to perform the smallest,
  fastest clone (no LFS, no submodules, no depth) possible. Includes parameters to
  override and modify the checkout process.

# This information will be displayed in the orb registry and is not mandatory.
display:
  source_url: "https://www.github.com/vsco/advanced-checkout-orb"
